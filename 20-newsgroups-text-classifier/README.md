# 20 Newsgroups Text Classifier

this project uses the 20 newsgroups dataset to train a text classifier that can predict the category of a given text. it uses tfidf vectorizer to convert the text into numerical features and svm classifier to make predictions. the model is trained and tested using the train_test_split function from sklearn.