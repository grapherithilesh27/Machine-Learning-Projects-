# Text Classification Model

A simple text classification model built using scikit-learn's 20 newsgroups dataset, it uses TF-IDF vectorization and a Multinomial Naive Bayes classifier, with hyperparameter tuning done using GridSearchCV