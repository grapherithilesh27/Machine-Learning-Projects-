# Text Classification with Newsgroups

This project uses the 20 newsgroups dataset to train a model that can classify text into different categories. The model uses tf-idf vectorization and a random forest classifier. You can train the model by running the python script, and view the classification report and accuracy score.