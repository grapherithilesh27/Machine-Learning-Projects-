# News Category Classifier

This project uses a naive bayes classifier to categorize news articles into different categories like sports, politics, and technology. It uses the newsgroups dataset from sklearn to train the classifier.

## Dependencies
- sklearn
- numpy
- pandas

## Usage
- Run the python script to train the classifier and print the accuracy.