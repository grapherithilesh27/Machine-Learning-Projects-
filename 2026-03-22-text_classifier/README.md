# News Article Classifier

This project uses machine learning to classify news articles into different categories. The model uses a svm classifier with tfidf features. The code is a bit messy but it works.

## Dependencies
- sklearn
- numpy
- pandas

## How to run
1. install dependencies
2. run the python script