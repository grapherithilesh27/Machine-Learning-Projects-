# Text Classification on Newsgroups Dataset

This project uses the newsgroups dataset from sklearn to train a text classifier. The goal is to classify text into different categories. The dataset is split into training and testing sets, and a Multinomial Naive Bayes classifier is trained on the training set. The accuracy of the classifier is then evaluated on the testing set.