# Handwritten Digit Recognition

This project uses a convolutional neural network to recognize handwritten digits from the MNIST dataset. It covers the basics of image preprocessing, batch normalization, and convolutional neural networks. The code is written in a way that is easy to understand and modify, with leftover debug print statements to help with debugging.